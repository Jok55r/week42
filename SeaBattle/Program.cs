namespace SeaBattle
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            Lobby lobby = new Lobby();
            lobby.StartGame();
        }
    }
}